<%= form_for :picture do |f| %>
  <% if @picture.errors.any? %>
    <div id="errorExplanation">
      <h2><%= pluralize(@picture.errors.count, "error") %> prohibited
          this picture from being saved:</h2>
      <ul>
      <% @picture.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :list %><br />
    <%= f.text_area :list %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>